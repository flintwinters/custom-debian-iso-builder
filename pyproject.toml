[project]
name = "helix"
version = "0.1.0"
description = "Build Helix Debian installer ISOs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "rich>=14.2.0",
    "typer>=0.26.7",
]
